{
  "name": "firebase-rolling-log",
  "version": "0.2.0",
  "description": "Captures a rolling Firebase log.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "firebase",
    "logging"
  ],
  "author": "Michael Lehenbauer <mikelehen@gmail.com>",
  "license": "MIT",

  "//": "We intentionally don't list 'firebase' as a dependency, because we don't want our own copy, which would end up with different logs.",
  "dependencies": {
  },

  "repository" : {
    "type" : "git",
    "url" : "https://github.com/mikelehen/firebase-rolling-log"
  }
}
